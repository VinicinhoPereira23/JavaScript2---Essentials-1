function exibirMensagem() {
    var mensagem;
    if (true) {
        let mensagem;
        console.log(mensagem); // ?
        console.log(mensagem); // ?
    }
    console.log(mensagem); // ?
    console.log(mensagem); // ?
}
exibirMensagem();

//a saída será indefinida